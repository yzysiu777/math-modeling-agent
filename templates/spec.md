---
spec_id: SPEC-<子问题>-<路线 ID>
case_id: <case_id>
route_id: M-
subproblem: <问题 1>
method_family: <方法族>
status: full
language: python
probe_exp_id: EXP-
probe_result: PASS
probe_waiver_reason: ""
# 仅当 probe_exp_id 指向 reviewer_probe 时必填；冻结 Full 前复算。
reviewer_probe_recheck_exp_id: ""
---

# <路线名称> Full SPEC

## 1. 目标与判据

- 要回答的问题：
- 主指标与方向：
- 通过/失败判据（**两栏都必须填，两栏都过才算通过**）：
  - 绝对底线：低到什么程度就说明这个模型没有实用价值。按本题的实际用途定，
    不要按惯例填一个看起来合理的数；
  - 相对增量：相对指定对照的增量门槛。没有对照时写 `n/a` 并说明为什么没有。
- 两级指标：路线含中间产物（预测、估计、分类、聚类等任何不是最终交付物的量）时，
  同时给出**中间量指标**与**最终交付指标**。中间量变好而交付指标不变或变差，
  是重要结果，照实记录，不许只报其中一个；不含中间产物写 `n/a`；
- 选择期与评估期：调参、选窗、选口径用的是哪一段数据，评估用的是哪一段，分别写明。
  两者重叠时必须写明「本指标偏乐观」，并把这句话带进论文；
- 统一比较范围：

## 2. 数学模型

- 集合、参数、变量及单位：
- 目标函数：
- 约束或统计关系：
- 模型边界：

## 3. 数据契约与显式假设

- 输入文件、字段、粒度和单位：
- 本题 `数据范围.md` 白名单及使用的前题 outputs：
- 切分、缺失、异常和重复处理：
- 本题适用的单位、坐标系、时间基准、网格对齐、缺测语义（不适用写 N/A）：
- 禁止使用的信息：

## 4. 算法与输出

- 算法/伪代码、关键参数和终止条件：
- 代码入口：
- 数据输出：`outputs/data/`
- 图表输出：`outputs/figures/`

## 5. 复算与遗留假设

- 独立复算项及阈值：
- 复算报告：`outputs/checks/`
- 敏感性/稳健性：
- Reviewer Probe 是否影响本规格；若是，对应复算 EXP-ID：
- 仍未验证但不阻塞运行的假设：
