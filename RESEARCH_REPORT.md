# 数学建模 Agent 核心架构报告

## 结论

Codex 主解、Claude 人工触发的独立挑战和确定性数值检查适合华为杯四天赛程。工业
级别应体现在模型、算法、实验和论文质量，而不是把每个普通动作变成行政流程。

当前工作台因此采用四层协作：

1. **主建模层**：Codex 完成题意重构、路线池、代码、实验、结果和论文初稿；
2. **方法挑战层**：Claude 只在 C1 题意、C2 架构、C3 结果三个关键节点人工触发；
3. **确定性检查层**：路由、数据切分/泄漏、约束可行性、目标复算、模型比较和 PDF QA；
4. **人工决策层**：队员决定关键歧义、路线取舍、强结论、官方规则和最终提交。

## 支持的题型

### 运筹优化

适用于规划、网络、路径、调度、资源分配、多目标、鲁棒和随机优化。优先验证变量、
目标、约束、单位、可行性、复杂度和最优性边界；先用小实例、手算或穷举建立基线。

### 数据分析

适用于清洗、统计推断、回归、分类、聚类、时间序列、预测、解释性和不确定性分析。
优先验证粒度、主键、标签、缺失、异常、切分、泄漏、基线、指标、校准和稳定性。

### 混合建模

当分析输出预测、参数、情景或概率并进入决策模型时，案例说明记录字段、类型、单位、
时间语义、误差/情景规则和下游可行性影响；同时比较拆分模块和端到端 baseline。

## 核心工作法

- 每个关键子问题至少提出三条方法论不同的候选路线；
- 先从运筹、数据分析和混合建模方法卡生成路线，再用题面和小实验筛选；
- 先跑成本低、信息价值高、可手算或小规模的实验；
- 用统一切分、实例、指标和资源预算公平比较；
- 保留 Champion 与方法论不同的 Challenger；
- 失败路线只留下可复用的代码、原因和最小证据；
- 每轮稳定结果都同步到问题—模型—实验—图表—结论—引用映射；
- 将“可行”“当前算法最好”“给定假设下最优”“全局最优”分开表达。

## Claude 的独立性

C1 只看题意契约，C2 看模型架构和算法，C3 看主要结果和论文强结论。审核包只含
完成节点所需的摘要、候选路线、公式/伪代码、实验和当前担忧。Claude 必须给出不同
方法族、反例或区分实验、已检查/未检查内容和人工决策项；它不重跑整题、不直接改主
方案，队员在 `decisions.md` 记录是否采纳。

## 论文与格式

`paper/` 使用 XeLaTeX、ctex、biber 和统一 `.bib`；`writing/` 固定证据化语言、图表
公式、引用、AI 使用记录和最终 PDF 检查。历史官方文件只作训练参考，比赛日必须重新
获取并核对当届标准、封面/摘要页、匿名、字体、页数和 AI 规定。

## 当前边界

工作台不内置任何历年题目答案，不自动调用模型 API，不要求普通实验先填写复杂记录。
原始输入只读保存，秘密配置和私有数据不进 Git。脚本报告证据与风险，不能替队员
理解题面、选择比赛策略或签署最终提交。
