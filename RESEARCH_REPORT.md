# 华为杯数学建模 Agent 架构实施报告

## 结论

“Codex 主解 + Claude 人工触发审核”适合数学建模竞赛，但只有在确定性验证器和人工门禁存在时才足够可靠。Claude 的价值是提供不同的模型先验、反例和审查视角，不是通过投票产生真值。

本工作台因此采用四层架构：

1. **主解层**：Codex 完成题意重构、建模、代码、实验和论文初稿；
2. **独立审查层**：Claude 只在 C1 题意、C2 模型算法、C3 结果强结论三个关键节点人工触发；
3. **证据层**：Schema、数据质量检查、约束检查、求解器、统计指标、复现脚本和 PDF QA；
4. **人工层**：人工批准目标、模型取舍、审核修订和最终提交。

## 支持的题型

### 运筹优化

适用于线性/整数/非线性规划、网络优化、路径规划、调度、资源分配、多目标、鲁棒和随机优化。优先验证变量、目标、约束、单位、可行性、复杂度和最优性边界。

### 数据分析

适用于数据清洗、统计推断、回归、分类、聚类、时间序列、预测、解释性和不确定性分析。优先验证数据粒度、主键、标签、缺失、异常、切分、泄漏、基线、稳定性和关联/因果边界。

### 混合建模

当数据分析产生预测、参数、情景或概率，后续优化负责决策时，必须使用 `hybrid_interface_contract` 明确字段、单位、误差、时间点、允许的更新方式和下游约束。

## 工业级质量原则

- 先冻结输入，再建立问题契约；
- 先做可解释 baseline，再比较复杂方法；
- 每条强结论都有来源、推导、程序、实验或人工决定；
- 每次改动都有版本和影响记录，并按 R0–R3 只执行受影响的定向回归；
- 失败路线、反例、不确定性和审查意见不可隐藏；
- P0/P1 未解决时停止向最终论文推进；
- 作者、审核者和最终批准者必须分离。

## 证据强度

| 结论类型 | 最低证据 |
|---|---|
| 数据定义 | 题面/字段说明、来源和数据质量检查 |
| 数学推导 | 假设、变量、公式、维度和边界检查 |
| 算法性质 | 伪代码/源代码、小例子、单元测试或理论说明 |
| 可行性 | 约束检查器输出 |
| 全局最优 | 严格求解器证书、小规模穷举或明确限定为启发式 |
| 预测性能 | 正确切分、基线、指标、稳定性和限制 |
| 因果结论 | 设计或假设足以支持因果，否则只写关联 |
| 可复现性 | 输入哈希、代码版本、依赖、命令、环境和输出 |

## Claude 交接

Claude 采用“先审后改”：先生成不可覆盖的 C1/C2/C3 审核记录，人工批准发现后再输出受限 patch。审核包使用文件白名单和哈希清单，不包含 Codex 隐藏推理。修订不能直接进入 `main`，且不得由 Claude 自行关闭高风险问题。

## 论文和格式

论文由 `paper/` 下的 XeLaTeX 工程构建，使用 `biber` 和 `gb7714-2015` 参考文献样式。`writing/` 固定内容结构、证据化语言、图表公式、引用、AI 使用记录和最终 PDF QA。`writing/official/2025/` 仅为历史快照；比赛日必须重新冻结当届官方标准。

## 当前边界

本仓库只服务华为杯竞赛，但不内置任何具体历年题目的路由或答案，也不自动调用模型 API。案例数据、秘密配置和私有审核包默认不提交 Git；可交付源代码、论文源文件、实验摘要、来源清单和审核报告可提交 Git。

## 实施边界与人工责任

可信运行器目前只自动执行确定性、低风险的固定检查；小规模手算、实验重跑、
模型正确性、论文图表语义和 PDF 视觉质量仍可能记录为 `manual_required`。这不是
自动通过，而是明确的人工责任清单。证据图和 G12 signoff 只接受实际文件与哈希，
不能由模型文本替代。案例级工作项的 `accepted` 也不代表最终论文冻结；最终决定
仍归 `human_owner`。
