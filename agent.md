# 竞赛共同执行协议

## 推进原则

一次只推进当前 `q<k>`；先产出可运行结果，再增加复杂度。可逆选择由当前 AI 直接决定并记录，
只有官方材料硬冲突、授权扩张和最终提交回人工。角色之间只传落盘材料，不传隐藏推理。

## 每题四步与一次收官

| 步骤 | 单一产出 | 结束条件 |
|---|---|---|
| A 定题 | `q<k>/brief.md` | C1 有节点决定、推荐动作和路由 |
| B 试跑 | `q<k>/board.md` + 至少两条路线各一份 Full SPEC | 判据两栏俱过，`spec-check` 通过；不过先换路，不是先降级 |
| C 出结果 | `q<k>/outputs/` | Champion 关键结果独立复算通过 |
| D 写本题 | `paper/sections/q<k>.tex` + claim map 行 + 本题 PDF | 关键 Claim 可追溯且编译通过 |
| E 收官 | 提交 PDF | 全案例一次 C3、最终检查和人工提交 |

后题可以使用前题 outputs，前题不得依赖后题。返工更新原文件和 `q<k>/log.md`，不建立 v2、
阶段报告或重复交接副本。

## 审核往返

C1、C2 每题必做；C3 每题一次，全案例收官前再一次。Reviewer 必须给唯一推荐动作、理由、
次优项和 `默认执行`。采纳时生产角色直接实施；拒绝 finding 才回到原 Reviewer 会话回签一次。
`GO_WITH_FIXES` 是可执行状态，不因 Reviewer 没有额外写“批准”而停工。

## 工程与证据

单位、坐标、时间、网格、缺测、切分和对齐写进实验板并做最便宜的敏感性或对照。
Full SPEC 保持五段；论文数字来自 board、outputs 和复算报告；缺证据时降低表述强度。

## Agent 回报卡

```text
[Q2 / 步骤B / DONE] 一句话结论
产物：<路径>
风险：<一条，或 none>
需要人工：否 / <具体问题>
下一步：<角色> <任务>
```

跨角色交接时可在这六行后补充：必须读取的文件、不可触碰范围、完成标准和复现命令；普通每轮
不得展开成多节汇报。
