# 竞赛共同执行协议

本文件只保留所有角色都需要知道的规则；角色细节在 `prompts/`。开工时不要把本文件、
三个 contracts 和全部 writing 规范重复塞给每个角色。

## 推进原则

1. 先产生可运行结果，再增加复杂度；
2. 一次只解决当前最有信息价值的问题；
3. 可逆且范围明确的选择由当前角色直接决定；
4. 只有官方冲突、授权扩张和最终提交回到人工；
5. 同一角色跨阶段复用会话，生产角色之间只靠落盘材料交接。

## 七阶段与节点

| 阶段 | 核心产出 | 结束条件 |
|---|---|---|
| 1 题意重构 | `case_brief.md`、输入清单、AI 路由 | C1 给出节点决定 |
| 2 发散收敛 | `models/candidates.md` | 至少三条方法论不同路线及 Probe 顺序 |
| 3 Probe | `experiments/board.md`、小代码、小结果 | 至少一条路线获得可执行判断 |
| 4 Full 与赛马 | Full SPEC、Baseline、统一比较 | C2 给出节点决定并确定 Champion/Challenger |
| 5 复算出图 | 关键结果、检查报告、图表 | Champion 关键结果复算通过 |
| 6 论文溯源 | LaTeX、关键 Claim | C3 给出节点决定 |
| 7 最终检查 | 提交 PDF | 队员人工确认并提交 |

## Reviewer 往返

审核卡既是输入也是唯一详细记录。Reviewer 输出 `GO`、`GO_WITH_FIXES` 或 `STOP`。
采纳意见后生产角色直接实施；若生产角色要拒绝某条 finding，必须把理由写回同一张卡，
由原 Reviewer 回签 `ACCEPT_REJECTION` 或 `REJECT_REJECTION`。除拒绝 finding 外，
不增加二次审批。

Reviewer 跑出的 Probe 可以直接登记为探索证据。若它改变 Full SPEC、Champion 或正式
数据口径，生产流程须在冻结 Full SPEC 前复算；若只提供后续线索，可留到相应路线进入赛马时。

## 工程自主边界

Engineer 不因一般规格空白停工。库、数据结构、容差、初始化、默认参数、插补、切分和对齐
优先采用明确且可重跑的方案，并记录影响。涉及单位、坐标系、时间基准、网格对齐或缺测语义
时，必须在实验板写一行显式假设并进入 C2 清单，但不自动等待人工。

## 检查原则

- 探索阶段的检查只提醒；
- C1/C2/C3 缺少节点决定时，在相应边界停下并自动补做审核；
- Reviewer 返回 `STOP` 时先由 AI 补证据、换路线或降低结论；
- 确定性不可行、目标复算错误、泄漏、切分重叠会阻止强结论；
- 小改只重跑受影响的实验或 LaTeX；完整 `make final-check` 只在最终提交前运行。

案例控制文件是进度提示，不是身份认证、审批证明或防伪系统。不要增加哈希流水线、
修订分类状态机或重复签字。
