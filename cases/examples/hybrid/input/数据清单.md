# 数据清单

数据保持原位只读。按「目录 × 列结构」分组：同一目录下列结构相同的文件合并成一行，每一种列结构在下方 `## 列结构` 中完整列出一次，不截断。

| 数据根 | 目录 | 文件数 | 合计大小 | 行数 | 列结构 | 编码 | 缺测标记候选值 |
|---|---|---:|---:|---|---|---|---|
| data | 第一题 | 1 | 8 B | 2 | C01 | utf-8-sig | 未发现 |

## 列结构

### C01 —— 1 个文件，例如 `data/第一题/toy.csv`

[col_1]

## 说明文档中的字段声明

- 未自动识别；请打开 `input/说明文档/` 核对无表头记录。
