\section{问题重述与总体思路}

\paperexample{这一节是评委判断「你有没有读懂题」的地方，**必须写细，不能一笔带过**。
每个子问题单独一小节，逐项写清：研究对象、给定的输入数据及其粒度、要求输出什么、
评价目标、硬约束、以及题面中容易读错或存在歧义的地方和你的取定。}

\subsection{问题一}
% <<Q1-SEALED>> 问题一完成 A 步并通过 C1 之前，本小节保持原样。
% 通过 C1 后删掉这一行再写。

\paperexample{输入 / 输出 / 目标 / 硬约束 / 歧义与取定，逐项展开。}

\subsection{问题二}
% <<Q2-SEALED>> 问题二完成 A 步并通过 C1 之前，本小节保持原样。
% 通过 C1 后删掉这一行再写。

\subsection{问题三}
% <<Q3-SEALED>> 问题三完成 A 步并通过 C1 之前，本小节保持原样。
% 通过 C1 后删掉这一行再写。

\subsection{总体解题思路}

\paperexample{按题增量写：每完成一题的 A 步并通过 C1，补一段该题的技术路线。
**不要替还没开工的题预先设计方案** —— 路线要从该题的发散与比较里长出来，
再经独立审核挑战，不是在这里猜出来的。}

% <<Q1-OUTLOOK-SEALED>> 问题一通过 C1 前保持原样。
% <<Q2-OUTLOOK-SEALED>> 问题二通过 C1 前保持原样。
% <<Q3-OUTLOOK-SEALED>> 问题三通过 C1 前保持原样。

\paperexample{全部子问题就位后，用一张总体技术路线图说明它们之间的关系：
数据如何流动、前一问的结论如何被后一问使用。缺了要用占位图挡住并登记。}

\begin{figure}[H]
  \centering
  \PlaceholderFigure[0.9]{三个子问题之间的数据流与结论传递关系。人工绘制后
    替换为 \texttt{\textbackslash includegraphics}，并从待补图清单中删去对应行。}
  \caption{总体技术路线}
  \label{fig:overall-roadmap}
\end{figure}
