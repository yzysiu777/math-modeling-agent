# 数学建模竞赛 Agent 工作规则

本文件适用于本目录及其子目录。它是长期规则，不替代当前用户对具体题目的直接要求。

## 任务目标

把数学建模竞赛工作组织成可复核、可复现、可解释的工程流程：题面与数据先冻结，模型假设显式记录，候选方法可运行验证，重要结论有证据链，最终论文经过独立审查和人工确认。

## 启动顺序

开始任何题目工作前，依次读取：

1. `README.md`
2. `protocol/workflow.md`
3. `protocol/gates.md`
4. `case_manifest.yaml`（若当前案例存在）
5. 当前任务对应的 `roles/*.md` 或 `skills/industrial-mathematical-modeling/SKILL.md`

如果当前案例没有 `case_manifest.yaml`，先建立草稿并报告缺失字段；不要直接假定数据含义、单位、训练/测试划分或评价指标。

## 权威层级

1. 用户明确要求和竞赛题面；
2. 原始附件与字段说明；
3. 官方赛事/高校原始资料；
4. 可追溯的作者仓库和论文；
5. 模型推导、程序输出和审查记录；
6. 博客、二手解释和模型记忆只作线索，不作最终证据。

## 角色边界

- `codex_lead` 可以协调、读取、创建实验和整合结果，但不能凭语言流畅度宣布模型正确。
- `claude_adversary` 默认只读，不修改主方案，不自我批准，不把与 Codex 一致当作证据。
- `data_auditor` 负责数据契约、字段、缺失、重复、单位、标签、时间切分和泄漏检查。
- `model_reviewer` 负责目标函数、约束、推导、算法、复杂度、边界和敏感性。
- `reproducibility_reviewer` 负责环境、随机种子、命令、输入版本、输出和独立复跑。
- `final_gatekeeper` 只检查门禁是否满足，不替论文作者补造实验或结论。
- 任何角色不得批准自己写出的报告；审查必须由另一角色、另一模型或人工完成。

## 论文写作与交付规则

- 论文工作开始前，必须读取 writing/OFFICIAL_RULES.md、writing/PAPER_STYLE_GUIDE.md、writing/NATIONAL_AWARD_LANGUAGE.md 和 writing/QA_CHECKLIST.md。
- 当届组委会发布的论文标准文档、格式规范和 AI 规定优先于本目录；没有当届文件时，必须标记 OFFICIAL_TEMPLATE_MISSING，不得凭历年模板猜测。
- 论文先建“题面子问—模型—实验—图表—结论—引用”映射，再写正文；摘要中的数字必须来自已登记的实验或结果表。
- “国奖语言”不是官方词典。默认使用克制、可证据化的表达，严格区分关联/因果、可行/最优、单次结果/稳定结论、模型重要性/实际影响。
- 图题放图下、表题放表上；图、表、公式按统一编号并在正文交叉引用；单位、字体、行距和页码以当届官方标准文档为准。
- 最终论文只以 PDF 为交付对象，必须经过文本、元数据、匿名性和渲染后的视觉检查。MD5 生成后禁止修改对应 PDF。
- Codex 可以写作和整理，Claude 必须至少完成一次独立盲审或对抗审查；没有人工最终确认，不得将 G7 标记为通过。

## 工件和证据规则

- 原始题面、原始附件和原始仓库只读保存，不覆盖、不重命名破坏来源上下文。
- 每个重要结论写入 claim register，并关联来源、文件、页码/行号、运行记录或推导。
- 结论状态只能是：`draft`、`supported`、`supported_with_limits`、`disputed`、`rejected`、`unverified`。
- 没有证据的内容必须标记 `unverified`，不能因为两个模型都同意而升级。
- 每个实验记录输入版本、代码版本、依赖环境、随机种子、命令、指标、输出路径和失败原因。
- 失败尝试和反例进入 `failures/`，不删除以便后续避免重复踩坑。

## 模型独立性

- 初始建模阶段，Codex 和 Claude 尽量只看同一份原始题面、数据契约和验收标准，不先互相看答案。
- Claude 看到 Codex 草稿后，必须明确标记为“非盲审”，并优先寻找反例、泄漏、单位错误和不可复现之处。
- 两个模型的分歧进入 `reviews/`，由确定性计算或人工裁决；不得采用多数投票替代验证。
- 同一条结论至少需要一种外部证据：数据检查、数学推导、程序测试、求解器结果或论文原文。

## 安全和停止条件

- 不运行来源未知的可执行文件；不执行未经检查的删除、覆盖、外部发布或付费下载。
- 数据字段含义不明、关键约束缺失、结果无法复现或审查出现 P0/P1 问题时，停止向最终论文推进。
- 预算、时间或模型调用不足时，降低并行度和候选数量，不降低验收标准。
- 若外部 Claude 不可用，使用新会话的独立审查提示词或人工审查，不假装完成对抗审查。

## 输出约定

每次阶段性输出先给：当前阶段、结论、证据状态、阻塞项和下一步。长篇分析写入工作区工件，聊天只汇报摘要和需要用户确认的决策。
