# 数学建模 Agent 总规则

本文件适用于工作台和复制了它的案例目录。它规定证据、权限、审核和交付边界，不替代用户对具体案例的明确要求。

## 总目标

把数学建模竞赛组织成可复核、可复现、可解释的工程流程：输入先冻结，问题先契约化，模型先 baseline 后复杂化，结论必须有证据，论文必须经过独立审核和人工签字。

## 启动顺序

开始案例工作前按以下顺序读取：

1. 工作台或案例根目录的 `AGENTS.md`、`README.md`；
2. `case_manifest.yaml`；
3. `protocol/workflow.md`、`protocol/gates.md`；
4. 当前路由对应的 `roles/` 和 `skills/`；
5. 论文工作再读取 `writing/OFFICIAL_RULES.md`、`PAPER_STYLE_GUIDE.md`、`NATIONAL_AWARD_LANGUAGE.md`、`AI_COMPLIANCE.md` 和 `QA_CHECKLIST.md`。

没有案例清单时，先建立草稿并标记缺失信息，不得默认字段含义、单位、标签、评价指标或训练/测试边界。

## 权威层级

1. 用户明确要求和当届官方题面；
2. 官方附件、字段说明和原始数据；
3. 官方赛事或高校原始资料；
4. 作者本人发布的论文、代码和实验；
5. 可复核的推导、程序输出和独立审查；
6. 博客、二手整理和模型记忆仅作检索线索。

来源不清的资料不能支持强结论。所有重要文件记录获取时间、来源、大小和 SHA-256。

## 角色边界

- `orchestrator` 负责阶段推进和证据完整性，不替模型证明结论。
- `task_router` 只负责题型判断和缺失信息识别，不擅自决定研究目标。
- `solution_lead` 负责主方案、实验和论文草稿，但不能批准自己的高风险结论。
- `optimization_modeler` 负责变量、目标、约束、算法、可行性和最优性边界。
- `data_analyst` 负责清洗、统计、预测、解释和不确定性。
- `data_auditor` 负责字段、切分、缺失、泄漏、单位和标签审计。
- `independent_adversary` 负责盲审、反例和对抗审查；默认只写审核记录。
- `reproducibility_engineer` 负责环境、命令、随机性、输出哈希和独立复跑。
- `paper_architect`、`citation_editor`、`formatting_qa` 分别负责结构、引用和版式。
- `final_gatekeeper` 只检查门禁，不替作者补造结果。
- `human_owner` 批准目标、模型取舍、审核修订和最终提交。

Codex 是默认主执行模型，Claude 是人工触发的独立审核模型；模型一致不能替代数据、推导、程序、求解器或人工证据。

## 通用路由

每个案例必须明确输出一个路由：

- `optimization`：决策变量和资源/约束是核心；
- `data_analysis`：数据生成、解释、预测或统计不确定性是核心；
- `hybrid`：数据分析的输出进入后续决策优化；
- `insufficient_information`：题面、数据契约或目标不足以安全建模。

路由不确定时先停在信息补全阶段，不用模型猜测替代题面。

## 证据与记录

- 原始题面和附件只读保存，不能覆盖、重命名到失去来源上下文或删除。
- 每个重要结论登记 `claim_record`，包含来源、实验、审查、状态、限制和最后修改者。
- 每个实验登记输入哈希、代码版本、环境、命令、参数、随机种子、指标、输出和失败原因。
- 失败尝试、反例和被否决路线进入 `failures/`，不得用删除来隐藏探索历史。
- 结论状态仅可为 `draft`、`supported`、`supported_with_limits`、`disputed`、`rejected`、`unverified`。
- “可行解”“当前算法最好解”“给定模型下最优解”“全局最优解”必须严格区分。
- “相关”“预测重要”“因果”“实际影响”必须严格区分。

## 固定竞赛质量门

阶段固定经过 G0–G12：目标/题意/权限，输入冻结，数据契约/假设/符号/指标，
baseline，正式模型/算法，正确性/可行性/边界，结果可信度/稳健性，独立审核，
修订/回归，论文证据绑定，官方格式/AI 合规，LaTeX/PDF 检查，人工冻结。

Gate 编号和状态以 `protocol/gates.md`、`protocol/workflow.md`、
`protocol/state-machine.md` 与 `scripts/gate_contract.py` 的同一份合同为准。

通过 Gate 后的改动必须进入 `gate_passed -> revision_pending -> impact_classified
-> targeted_validation -> validation_passed|validation_failed -> restore_affected_gate`
循环。R0–R3 只重跑受影响的检查，不得以形式签字替代退出证据。

P0/P1 问题未解决时停止向论文定稿推进。修改模型、数据、标签、目标、约束、求解器、随机种子或论文强结论后，必须创建新版本和新实验记录。G8 的闭环只接受可信运行器生成的结构化 `check_results`；缺失日志、日志哈希、输出哈希或运行器身份时 fail closed。证据图校验必须贯穿 G9、R1、R2、R3。

案例协作只在 `cases/<case_id>/coordination/` 记录工作项、审核和证据；一个工作项只有一个写入者，执行者不能担任该项 reviewer。`accepted` 不等于 `human_frozen`，G12 必须验证人类签字人、时间、当前 Git revision、最终 PDF 路径/哈希、完整 G0–G12 和无 P0/P1。

## 论文规则

- 当届官方论文标准和 AI 规定优先于本仓库；历史快照不能代替当届文件。
- 先建“子问题—模型—实验—图表—结论—引用”映射，再写长篇正文。
- 摘要和结论中的数字必须回溯到已登记实验或结果表。
- 公式首次出现定义全部符号；图题置于图下，表题置于表上，公式/图/表必须交叉引用。
- 最终交付以渲染后的 PDF 为准，检查匿名性、页码、字体、元数据、引用、AI 声明和版式。
- 最终 PDF 计算哈希后不得再修改；修改后必须重新编译、检查和计算哈希。

## 安全和停止条件

- 不执行来源未知的可执行文件、破坏性命令、付费下载或绕过访问限制。
- 不把密钥、Cookie、私有数据或隐藏推理提交到 Git、审核包或论文。
- 字段含义、关键约束、结果复现或官方格式缺失时，记录为阻塞项，不靠常识补齐。
- 外部审核模型不可用时，明确标记未完成，改用新会话或人工审查，不伪造审核结果。

## 阶段输出

每次阶段性汇报必须给出：当前 Gate、已支持结论、证据位置、未验证项、阻塞项、下一步和需要人工决定的事项。最终交接必须包括目录、运行/审查状态、关键结果、失败路线、论文格式状态、AI 使用记录和人工签字状态。
