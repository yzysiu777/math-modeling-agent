# 四天数学建模工作台规则

本仓库服务四天竞赛。一个案例装整道题，共享题面、数据来源和论文；每天围绕一个子问题走完
A–D，最后全案例执行一次 E。不要为流程完整性牺牲实际产出。

## 唯一流程

1. 阶段 0：按 `sources.yaml` 一次性转写题面、扫描说明文档、列全数据字段并生成每题白名单；
2. A 定题：本题 brief 合并题意、至少六个想法、至少三条路线、七维度和 Champion；C1 必做；
3. B 试跑：实验板保留 2–3 行 Probe，**至少两条路线**各写一份五段 Full SPEC；
4. C 出结果：正式实现、统一比较、Champion 独立复算和图表；C2 每题必做；
5. D 写本题：写本题一章与它对应的前置小节、更新 claim map 与文献清单、编译本题 PDF；
6. E 收官：全案例只做一次 C3、最终检查、人工确认与实际提交。

`make case-check` 的四个 STAGE 值不变；它们与 A–E 的对应关系见 README。没有另一套普通、
快速、研究或发布模式。

## 范围就是目录

当前角色只能写 `<案例>/q<k>/` 中本题文件及明确的共享论文文件。`q<k>` 可读取
`q<j>/outputs/` 当且仅当 `j < k`；引用后续题产物会被
`CROSS_QUESTION_BACKWARD_REFERENCE` 阻断。原始数据严格按 `q<k>/数据范围.md` 只读。

## 持续角色

| 角色 | 会话 | 主要职责 |
|---|---|---|
| Orchestrator | 全案例一个持续会话 | 阶段 0、当前题推进、审核卡、共享论文收官 |
| Modeler | 每题一个持续会话 | A、B：题意、路线、Probe、Full SPEC |
| Engineer | 每题一个持续会话 | B、C：正式实现、复算、图表 |
| Writer | 每题一个持续会话 | D 与共享 claim map |
| Independent Reviewer | C1/C2/C3 各自新会话 | 方法论挑战、唯一推荐动作和节点决定 |

生产角色固定使用 `gpt-5.6-sol`、`high`，均由队员人工启动；不可用时报告，不自动降级。
Reviewer 由队员人工在上下文隔离的新会话启动，provider/model 如实记录，不接任何模型 API。

## 一切为题目服务

`q<k>/brief.md` 的**交付物契约表**（形态/量纲/覆盖范围/验收方式）是全题的锚点：
判据、降级、论文都对着它核对。C1 挑战它录全没有、验收方式定得有没有意义。

判据分**绝对底线**与**相对增量**两栏，两栏都过才算通过 —— 只过相对项会选出最不烂的那条。

**判据不过时降级是最后一步**：先做合成对照自查（失败出在实现还是数据），再回候选路线表
找能保住交付物形态的替代路线（换路不是降级），一条都没有才降级，且必须逐条写明每条剩余
路线为什么也保不住形态。**形态优先于范围** —— 冲突时保形态、缩范围，缩小如实标注。

只有降级后判为「不满足题面要求」时才出批准单惊动队员，仍满足的记一行继续。

细则在 `templates/experiment_board.md`、`prompts/modeler.md` 与 `protocol/competition-workflow.md`。

## 论文是交付物

案例论文工程由 `create_case` 播种，用官方版式 `gmcmthesis`，按问分章。**不得新建主文档
或更换文档类。**每题 D 的收尾条件是 `make paper CASE=<案例> Q=q<k>` 编译通过。

正文只写论文该有的东西：实验编号、文件路径、命令行、检查器码和超过四位有效数字都不进
正文，溯源留在 `paper/claim_map.md`；草稿要标来源用 `\dataref{}`，它默认不打印。

外部事实要有出处。文献两条通路 —— 队员放进 `sources.yaml` 的 `literature` 文件夹，或由
写作手联网检索 —— 都登记进 `paper/文献清单.md`，写清支撑论断。核对状态是队员的抽查清单，不阻断流程；
但引用的 key 必须能在清单和 bib 里查到，这一条是机械的。
引用的 key 必须同时在清单和 `references.bib` 里，否则判编造。

没通过 C1 的子问题一律封存：各章按问小节带 `% <<Qk-SEALED>>` 标记，标记在就不许写内容，
删标记同样违规。路线要从该题自己的 A 步长出来，不是在别的题里猜。

行文按论文规范：列表只用于模型假设、符号说明和程序清单，每题正文至多一个；禁用
「如图所示」这类套话。画不了的图用 `\PlaceholderFigure` 占位并登记 `待补图清单.md`。

## 启动提示词由脚本生成

```bash
make start-prompt CASE=<案例目录> ROLE=<角色> Q=q<k>
```

**不要手写。**规则在 `prompts/`，事实在案例里，提示词只做填空和指路；手写就会把题目结论
抄进去，抄一次就多一处会过期的副本。

## 三个审核节点

- C1 每题必做；卡中有节点决定且写明推荐路由，即完成该题路由确认；
- C2 每题必做，审核卡带上当前实现代码与已落盘的复算报告 —— 审核者要看得到实现与规格
  是否真的对得上。检查器的三条风险条件（Champion 没有 done+PASS Probe、同一路线至少
  两行 failed、Full SPEC 的 `probe_result` 为 `WAIVED` 或 `PENDING`）不再决定要不要审，
  只作为审核卡「本轮最担心什么」的输入；
- C3 每题 D 结束一次，全案例收官前再一次，抽 3–5 条最高风险 Claim。

Reviewer 每条 finding 必须写唯一 `推荐动作：`、理由和次优项，并标注 `默认执行`。队员不回应，
生产角色按推荐实施；队员仍可否决。Reviewer 不修改主解文件、不启动其他 Agent、不代写论文。
`Human-only block` 只允许官方材料硬冲突、授权范围扩张、最终提交三类事项。

## 轻量证据

- Probe 先写假设、判据、范围和预算，失败照实保留；
- Champion 约束、目标值、切分和泄漏独立复算；
- `paper/claim_map.md` 只追踪摘要、结论和核心图表的关键数字；
- `decisions.md` 只记人工专属决定；
- 每题 `log.md` 是唯一过程记录，每步追加四行：做了什么、产物路径、风险、下一步。

## Agent 回报

每轮只使用 `agent.md` 的六行回报卡。完整版仅在跨角色交接时展开。脚本通过只代表相应结构
检查通过，不代表模型正确；最终状态必须区分实现证据与独立验收。
