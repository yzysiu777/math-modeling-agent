# 华为杯数学建模工作台规则

本仓库只服务于四天竞赛。默认目标是：尽快形成多条可比较路线，运行便宜 Probe，
用实验收敛到可交付模型，并同步完成论文。不要为流程完整性牺牲实际产出。

## 唯一流程

1. 案例启动与题意重构，结束后 C1；
2. 发散、七维度比较与路线收敛；
3. 分段 Probe；
4. Full 规格、Baseline 与路线赛马，冻结架构前 C2；
5. Champion 独立复算与出图；
6. 论文与关键数字溯源，写强结论前 C3；
7. 最终数值、LaTeX、官方格式与提交检查。

没有 ordinary、fast、research 或 release 模式，也没有比赛/研究开关。需要更深入时，
在当前阶段追加实验，不切换另一套流水线。

## 持续角色

| 角色 | 会话 | 主要职责 |
|---|---|---|
| Modeler | 一题一个持续会话 | 题意、发散、路线、Probe、Full 规格 |
| Engineer | 一题一个持续会话 | 正式实现、实验、复算、图表 |
| Writer | 一题一个持续会话 | LaTeX、关键 Claim、语言与版式 |
| Orchestrator | 主会话持续运行 | 推进阶段、调度生产 Agent、准备 Claude 交接 |
| Independent Reviewer | 人工调用外部 Claude | C1/C2/C3 方法论挑战和节点决定 |

生产角色之间不传递隐藏推理，只传案例中的落盘文件。同一角色跨阶段继续工作时复用原会话，
不重复读取整套协议。Skill 和 contracts 都按需读取，不是开工前必读清单。

## 固定模型与调度权限

Orchestrator、Modeler、Engineer、Writer 统一使用 `gpt-5.6-sol`，推理强度为 `high`。
Orchestrator 创建生产 Agent 时必须显式指定该配置；不可用时报告人工，不自动降级。

C1/C2/C3 不使用上述生产模型。Orchestrator 只生成审核卡和提示词，由队员人工调用新的
外部 Claude 会话。Claude 的实际型号如实写入审核卡；Orchestrator 不得自行启动 Reviewer，
也不得把 Claude 审核静默替换成 Codex 审核。

## AI 默认自主

AI 直接决定路由、路线顺序、Champion/Challenger、指标、参数、切分、缺失值、对齐、
求解器设置、审核意见处置和论文表述强度。决定应可见、可复现；证据不足时采用保守口径。

只有三类情况交给队员：

- 官方题意、格式或 AI 规则存在无法由官方材料消除的冲突，并改变硬约束或交付物；
- 需要扩大已授权的数据、目录、网络、登录或付费范围；
- 最终参赛文件确认与实际提交。

Engineer 遇到单位、坐标系、时间基准、网格对齐或缺测语义选择时，不停工回问；先在
`experiments/board.md` 写成显式假设，运行敏感性或对照，并把它列入 C2 必查项。

## 三个短审核节点

C1/C2/C3 是纠偏，不是审批流水线。Orchestrator 到点自动生成审核卡和可复制提示词，
然后明确提示队员人工调用外部 Claude。
审核卡结论只用 `GO`、`GO_WITH_FIXES`、`STOP`。

- 采纳 finding：对应生产角色直接实施，零往返；
- 拒绝 finding：必须由 Reviewer 在同一张审核卡回签一次；
- Reviewer Probe 若影响 Full SPEC 或 Champion，必须在 Full SPEC 冻结前由生产流程复算；
- `STOP` 只有命中人工专属边界时才等待队员，否则 AI 自行换路线、补证据或重做。

## 轻量证据

- Probe 的假设、预设判据、预算、结果和去留写在实验板一行，不建独立 Probe SPEC；
- Full SPEC 只给进入正式赛马的路线，保持五段；
- Champion 的关键约束、目标值、切分和泄漏必须独立复算；
- `paper/claim_map.md` 只追踪摘要、结论和核心图表中的关键数字；
- 普通表格由数据文件生成，不逐个数字登记；
- `decisions.md` 只记录人工专属决定，不誊录普通 AI 取舍。

## 最低纪律

- 原题、附件和官方说明保持只读；未知内容不补造成事实；
- 发散至少六条想法，收敛至少三条方法论不同路线；
- Probe 必须先写判据再运行，失败照实保留；
- 不把启发式写成全局最优，不把关联写成因果；
- 脚本通过只代表相应检查通过，不代表模型整体正确；
- 小修改只做受影响检查，最终提交前再运行一次完整检查。

## 统一回报与阶段报告

每个生产 Agent 每轮结束都按 `agent.md` 的“Agent 回报卡”输出：阶段、角色、模型、本轮结果、
风险、人工核验建议、下一步 Agent 和按需交接提示词。只有 Orchestrator 写阶段报告：

```text
reports/stage-01.md ... reports/stage-07.md
```

阶段 1–6 报告供队员异步学习和排查，不因尚未阅读而停工；发现问题后定向退回对应阶段。
阶段 7 的最终 PDF 和实际提交必须等待人工。返工更新原阶段文件的修订记录，不建 v2 副本。
