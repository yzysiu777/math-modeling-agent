# 数学建模 Agent 总规则

本文件适用于工作台和复制了它的案例目录。规则追求快速探索与可复核证据的平衡，
不把普通建模工作变成审批项目。

## 七条硬规则

1. 先读题面和附件，再写 `case_brief.md`；缺失信息标为未知，不凭常识补成事实。
2. 原始输入只读保存；来源、获取日期、文件大小和可选 SHA-256 记录在案例说明中。
3. 每个关键子问题至少提出三条方法论不同的路线，不能只是同一模型换参数。
4. 先跑便宜、可解释、可手算的 baseline，再用统一数据切分、约束口径和指标比较。
5. 运筹输出必须独立检查可行性和目标值；数据输出必须检查粒度、切分、泄漏、指标
   和不确定性；混合题必须说明预测误差如何传入决策。
6. Claude 只在 C1、C2、C3 关键节点做独立挑战；它输出问题、反例和建议，不能代替
   队员决定，也不能把主方案的自洽当成外部证据。
7. 论文与实验同步推进；任何强数字、最优性、因果性和泛化结论都必须能回到实验、
   推导或题面来源，最终 PDF 以当届官方规则和人工检查为准。

## 默认角色

- Codex：题意重构、路线生成、代码、实验、结果和论文初稿；
- Claude：C1 语义挑战、C2 架构挑战、C3 结果和论文强结论挑战；
- 确定性脚本：路由、案例初始化、数据/模型基础检查和 LaTeX/PDF QA；
- 人类队员：目标、比赛策略、Champion 取舍、争议假设、Claude 建议和最终提交。

## 何时停下来问人

只有下列情况必须暂停并请求人工决定：题意歧义会改变目标或硬约束；Champion 与
Challenger 的差异无法由指标解释；Claude 找到会改变架构的反例；或需要确认官方
格式、匿名、AI 使用规定和最终提交文件。低风险的探索、代码、实验和排版修改可
由 Agent 直接推进并留下简短记录。

## 失败与不确定性

不要删除失败路线来美化结果。用 `experiments/board.md` 或 `decisions.md` 记录
失败原因、复现命令和下一步；区分“可行解”“当前算法最好解”“给定假设下的最优解”
和“全局最优解”，区分关联、预测重要性和因果。
