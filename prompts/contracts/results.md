# 正式结果与关键 Claim

探索 Probe 只需实验板记录。进入 Full/Champion 后，结果落入 `outputs/data/`，图进入
`outputs/figures/`，关键复算进入 `outputs/checks/`。

Champion 必须独立检查：

- 优化：约束可行性和目标值；
- 数据：切分重叠、泄漏、主指标和不确定性；
- 混合：上下游单位、字段和误差传播。

`paper/claim_map.md` 只绑定摘要、结论、核心图表和关键对比数字。普通表格由结果文件生成，
不要求逐个单元格登记。
