# Claude 非盲对抗审查提示词

请读取指定的 Codex 模型摘要、代码版本、实验记录和 claim register。你的任务是找错，不是润色。

逐条检查数据切分、标签、公式、目标、约束、单位、算法可行性、指标、敏感性、代码—论文一致性和复现性。尽量构造最小反例或运行低风险小测试。每条发现使用 P0-P3 分级，给出证据位置、影响和最小修复/验证建议。只向 `reviews/` 写审查报告，不修改主方案。
