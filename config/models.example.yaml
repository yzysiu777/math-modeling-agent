# 仅作角色和权限示例，不放 API key、Cookie、账号或真实私密地址。
lead:
  platform: codex
  model: PIN_LOCALLY_FOR_THE_COMPETITION
  reasoning_effort: high_or_max
  workspace_write: true
  workflow: fixed_competition_g0_g12
  roles: [orchestrator, task_router, solution_lead, paper_architect]

adversary:
  platform: claude
  model: PIN_LOCALLY_FOR_THE_COMPETITION
  workspace_write: reviews_and_failures_only
  initial_visibility: raw_input_and_contract_only
  nonblind_visibility: explicit_review_bundle_only
  roles: [independent_adversary]
  review_nodes: [C1, C2, C3]

deterministic:
  python: PIN_ENVIRONMENT_VERSION
  schema_validator: jsonschema_or_project_validator
  statistics: PIN_ENVIRONMENT_VERSION
  graph: PIN_ENVIRONMENT_VERSION
  optimizer: OPTIONAL_SOLVER_AND_LICENSE
  latex: XeLaTeX_biber_latexmk

human:
  approves_goals: true
  approves_route: true
  approves_model_tradeoffs: true
  approves_claude_revision: true
  signs_final_report: true
